server:
  port: 8080
  servlet:
    context-path: /hotel_reservation_premium

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/hotel_reservation_premium?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Bangkok
    username: root
    password: An_cat_m0i
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate.naming:
      physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

  sql:
    init:
      mode: never

# JWT Configuration
jwt:
  secret: hotel_reservation_secret_key_2024_very_long_and_secure_at_least_256_bits_for_hs256_algorithm
  expiration: 86400000  # 24 hours in milliseconds (24 * 60 * 60 * 1000)

